services:
  node1:
    image: flovenet:latest
    command: daemon --port 0 --api-port 9091 --roles compute,storage
    ports:
      - "9091:9091"
    environment:
      - RUST_LOG=info
    networks:
      - flovenet

  node2:
    image: flovenet:latest
    command: daemon --port 0 --api-port 9092 --roles compute
    ports:
      - "9092:9092"
    environment:
      - RUST_LOG=info
    networks:
      - flovenet

  node3:
    image: flovenet:latest
    command: daemon --port 0 --api-port 9093 --roles storage
    ports:
      - "9093:9093"
    environment:
      - RUST_LOG=info
    networks:
      - flovenet

  gateway:
    image: flovenet:latest
    command: api-gateway --port 8080
    ports:
      - "8080:8080"
    environment:
      - RUST_LOG=info
    networks:
      - flovenet

networks:
  flovenet:
    driver: bridge
